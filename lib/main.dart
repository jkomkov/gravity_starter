// lib/main.dart
import 'package:flutter/material.dart';
import 'main_app.dart'; // Imports the MainApp widget

// Main entry point for the application
void main() {
  // Runs the root widget of the app
  runApp(const MainApp());
}